PROGRAMA
    VARIABLES
        global_i, global_j, global_k:NUM;
        global_s:SEQ(NUM);
    SUBPROGRAMAS
        PROCEDIMIENTO test2 (NUM a)
            VARIABLES
                acum,limite:NUM;
            INSTRUCCIONES
                acum = a + 25;
                limite = 0;
                mientras (limite < 10) hacer
                    limite = limite + 1;
                    acum = a * acum;
                fmientras
                global_j = acum;
        FPROCEDIMIENTO
        FUNCION test (NUM a, NUM b) dev (NUM c)
            VARIABLES
                global_i2:NUM;
            INSTRUCCIONES
                c = a + b;
                dev c+1;
        FFUNCION
    INSTRUCCIONES
        test2(25);
        global_i = test(global_j,2);